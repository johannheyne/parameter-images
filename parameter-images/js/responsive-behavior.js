jQuery.noConflict();
jQuery(document).ready(function(){

	//jQuery('.resp').attr( 'data-resp', JSON.stringify(obj).replace(/\"/g, "\'") );
	
	// console.log( JSON.parse( jQuery('.resp').data('resp').replace(/\'/g, "\"") ) );

});